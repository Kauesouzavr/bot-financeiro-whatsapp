import { describe, expect, it } from "vitest";

// Teste de fumaça só pra confirmar que o Vitest está configurado.
// A Fase 2 substitui isso pelos testes reais do núcleo (parser, categorias, saldo).
describe("fundação", () => {
  it("roda testes", () => {
    expect(1 + 1).toBe(2);
  });
});
